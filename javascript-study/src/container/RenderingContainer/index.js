import React from "react";
import {RenderingContainerLayout} from "./style";

// props가 바뀌어서 렌더링된다고해도 원래 가지고있는 state가 바뀌는게 아님
const RenderingContainer = props => {
    return (
        <RenderingContainerLayout>

        </RenderingContainerLayout>
    )
}

export default RenderingContainer;