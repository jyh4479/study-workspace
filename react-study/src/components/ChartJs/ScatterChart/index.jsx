import React from 'react';
import {Scatter} from "react-chartjs-2";

const ScatterChart = (props) => {
    return (
        <Scatter data={[]} type={"scatter"}/>
    )
};

export default ScatterChart;